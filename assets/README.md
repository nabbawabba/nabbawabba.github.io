Place image files for the site in this folder.

Suggested names:

- `headshot-main.jpg` for the hero portrait
- `artwork.jpg` for the artistic rendition
- `poker.jpg` for the poker photo

After adding images, update `index.html` to replace the current placeholder blocks with `<img>` tags.
